@model IEnumerable<AppVendasWeb.Models.Categoria>

@{
    ViewData["Title"] = "Categorias";
}

<h1 class="display-5 text-light my-5">Categorias</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Criar Novo</a>
</p>
<table class="table table-dark table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoriaNome)
                </td>
                <td class="text-end">
                    <a asp-action="Edit" asp-route-id="@item.CategoriaId" class="btn btn-outline-warning">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.CategoriaId" class="btn btn-outline-info">Detalhes</a>
                    <a asp-action="Delete" asp-route-id="@item.CategoriaId" class="btn btn-outline-danger">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
